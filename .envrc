# 프로젝트 루트
cat > .envrc <<'EOF'
# uv venv 자동 활성화
if [ -d .venv ]; then
  source .venv/bin/activate
fi

# .env 파일을 쉘에 주입(필요할 때만)
if [ -f .env ]; then
  export $(grep -v '^#' .env | xargs)
fi
EOF

direnv allow
